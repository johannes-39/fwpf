apiVersion: v1
kind: Service
metadata:
  name: kubernetes-project
  namespace: develop
spec:
  selector:
    app: kubernetes-project
  type: NodePort
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 30000